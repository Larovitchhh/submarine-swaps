[project]
name = "submarine-swap"
description = "Demonstrates LNSwap's submarine swaps between Stacks and Bitcoin"
authors = []
telemetry = true
cache_dir = "./.cache"

[contracts.stx-submarine-swap]
path = "contracts/stx-submarine-swap.clar"
clarity_version = 4
epoch = "latest"

[repl.analysis]
passes = []

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
